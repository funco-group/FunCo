import NarrowLayout from '@/components/layout/NarrowLayout'

function NotesDetailLayout({ children }: { children: React.ReactNode }) {
  return <NarrowLayout>{children}</NarrowLayout>
}

export default NotesDetailLayout
